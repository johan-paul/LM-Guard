# Pinned to the exact versions verified working in this session (Python 3.13, Windows,
# CPU only). `pip install -r requirements.txt` reproduces a known-good environment.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
rapidocr-onnxruntime==1.2.3
opencv-python-headless==5.0.0.93
numpy==2.5.2
httpx==0.28.1
anthropic==1.4.0
python-multipart==0.0.32

# test-only, harmless in production
pytest==9.1.1
pytest-mock==3.14.0
