# Copy to .env for local development (Vite loads .env automatically, gitignored).
# Base URL of the LM-GUARD Spring Boot backend, including the /api prefix.
VITE_API_BASE_URL=http://localhost:8080/api
