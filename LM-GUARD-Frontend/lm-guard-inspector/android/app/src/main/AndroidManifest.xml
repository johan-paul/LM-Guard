<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- ARCore (Rule 7 numeral-height measurement, see ArMeasurementActivity.kt) needs live
         camera frames to track the device's pose in the world. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- A device without ARCore support (see uses-feature below) can still install and use
         every other part of the app - only the measurement feature checks availability at
         runtime and declines gracefully; this does not gate the whole install. -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false"/>
    <application
        android:label="lm_guard_inspector"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ArMeasurementActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait" />
        <!-- "optional": the app installs and runs fine on a device without ARCore support;
             ArMeasurementActivity checks ArCoreApk.checkAvailability() itself at runtime and
             reports "not supported" rather than assuming this meta-data alone guarantees
             anything (Play Store install-time filtering by this tag is best-effort, not a
             substitute for the runtime check). -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
