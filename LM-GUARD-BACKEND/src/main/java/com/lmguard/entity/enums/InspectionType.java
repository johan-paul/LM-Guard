package com.lmguard.entity.enums;

/** Why an inspection was opened. Mirrors the field application's workflow vocabulary. */
public enum InspectionType {
    ROUTINE,
    COMPLAINT,
    FOLLOW_UP,
    DRIVE
}
