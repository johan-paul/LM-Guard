package com.lmguard.entity.enums;

/** An inspector's own recorded outcome for one checklist line - never written by the AI. */
public enum CheckResult {
    PENDING,
    COMPLIANT,
    NON_COMPLIANT,
    NOT_APPLICABLE
}
