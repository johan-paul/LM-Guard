# Pinned to the exact versions verified working in this session (Python 3.13/3.14, Windows,
# CPU only). `pip install -r requirements.txt` reproduces a known-good environment.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-dotenv==1.1.0
rapidocr-onnxruntime==1.2.3
opencv-python-headless==5.0.0.93
numpy==2.5.2
Pillow==12.3.0
httpx==0.28.1
google-genai==2.22.0
python-multipart==0.0.32

# No longer imported by vlm.py (Gemini is the active VLM provider) -- left installable in case
# Anthropic credit is restored and the provider is switched back.
anthropic==1.4.0

# test-only, harmless in production
pytest==9.1.1
pytest-mock==3.14.0
